<?php
        if (empty($local_error)) {
          
        } else {
          foreach ($local_error as $key => $value) {

          }
        } 

        if (empty($main_error)) {
          
        } else {
          foreach ($main_error as $key => $value) {

          }
        } 
      ?>